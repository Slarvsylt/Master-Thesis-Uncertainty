using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProbabilityHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public bool DamageUnit(Unit target, float damage, float hitMod, float damageMod)
    {
        return false; // fail
    }

    public bool DamageUnit(Unit target, float damage, float hitMod, float damageMod, float otherModifier)
    {
        return false; // fail
    }
 
}
